const productRemove = document.getElementById('product_remove');
const productAdd = document.getElementById('product_add');
const prices = document.getElementById('total_price');

prices.addEventListener('click', function (e) {

})
